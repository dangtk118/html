<?php
$dir = "/var/www/html/static/tet/";
$baseUrl = "http://www.fahasa.com/static/tet/";
?>
<table id="duong-sach"  class="display responsive nowrap" style="width: 100%; word-wrap: break-word;" cellspacing="0">
    <thead>
        <tr>
            <th>&nbsp;</th>
            <th>Stt</th>
            <th>Tên</th>
            <th>Link hình ảnh</th>
        </tr>
    </thead>
    <tbody>
        <?php
        if (is_dir($dir)) {
            if ($dh = opendir($dir)) {
                $i = 0;
                while (($file = readdir($dh)) !== false) {
                    if ($file == '.' || $file == '..') {
                        continue;
                    }
                    ?>
                    <tr>
                        <td>&nbsp;</td>
                        <td><?php echo ++$i; ?></td>
                        <td>
                            <a target="_blank" href="<?php echo $baseUrl . $file; ?>">
                                <?php
                                echo str_replace(".jpg", "", $file);
                                ?>
                            </a>
                        </td>
                        <td><a target="_blank" href="<?php echo $baseUrl . $file; ?>">Xem ảnh</a></td>
                    </tr>
                    <?php
                }
                closedir($dh);
            }
        }
        ?>
    </tbody>
</table>